export const USER_ROLE = {
    student: 'student',
    superAdmin: 'superAdmin',
    admin: 'admin',
    faculty: 'faculty',
} as const;

export const UserStatus = ['in-progress', 'blocked'] as const;
